<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Modifier un contact</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet"
          integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6" crossorigin="anonymous">
</head>
<body>
<div class="container">
    <div class="row">
        <div class="col-10 card shadow mx-auto mt-3">
            <div class="card-header text-center bg-dark text-light mt-2">
                <h3>Modifier un contact</h3>
            </div>
            <div class="card-body">
                <form action="/contacts/edit/<%= contact._id %>" method="POST">
                    <div class="mb-3">
                        <label for="nom" class="form-label">Nom</label>
                        <input type="text" class="form-control" id="nom" name="nom" value="<%= contact.nom %>" required>
                    </div>
                    <div class="mb-3">
                        <label for="prenom" class="form-label">Prénom</label>
                        <input type="text" class="form-control" id="prenom" name="prenom" value="<%= contact.prenom %>" required>
                    </div>
                    <div class="mb-3">
                        <label for="societe" class="form-label">Société</label>
                        <input type="text" class="form-control" id="societe" name="societe" value="<%= contact.societe %>" required>
                    </div>
                    <div class="mb-3">
                        <label for="adresse" class="form-label">Adresse</label>
                        <input type="text" class="form-control" id="adresse" name="adresse" value="<%= contact.adresse %>" required>
                    </div>
                    <div class="mb-3">
                        <label for="tel" class="form-label">Téléphone</label>
                        <input type="tel" class="form-control" id="tel" name="tel" value="<%= contact.tel %>" required>
                    </div>
                    <div class="mb-3">
                        <label for="email" class="form-label">Email</label>
                        <input type="email" class="form-control" id="email" name="email" value="<%= contact.email %>" required>
                    </div>
                    <div class="mb-3">
                        <label for="secteur" class="form-label">Secteur</label>
                        <select class="form-control" id="secteur" name="secteur" required>
                            <option value="Industrie" <%= contact.secteur === 'Industrie' ? 'selected' : '' %>>Industrie</option>
                            <option value="Informatique" <%= contact.secteur === 'Informatique' ? 'selected' : '' %>>Informatique</option>
                            <option value="Santé" <%= contact.secteur === 'Santé' ? 'selected' : '' %>>Santé</option>
                            <option value="Education" <%= contact.secteur === 'Education' ? 'selected' : '' %>>Education</option>
                        </select>
                    </div>
                    <button type="submit" class="btn btn-primary">Modifier</button>
                    <a href="/contacts" class="btn btn-secondary">Annuler</a>
                </form>
            </div>
        </div>
    </div>
</div>
</body>
</html> 